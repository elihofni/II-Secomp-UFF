include ./../../components/spinner/spinner
include ./../../components/button/button

section.section
  h1.section-title Seção campo

  +addButton({ label: 'Botão 1' })
  +addButton({ label: 'Botão 2', extraClass: 'button--primary' })

  +addSpinner()
